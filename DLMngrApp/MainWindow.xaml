<Window x:Class="DLMngrApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DLMngr" Height="520" Width="900"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="DLMngr" FontSize="28" FontWeight="Bold" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Download folder:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="DownloadFolderBox" Width="420"/>
            <Button x:Name="BrowseFolderButton" Content="Browse" Width="80" Margin="8,0,0,0" Click="BrowseFolderButton_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Parallel downloads / domain:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="ParallelDownloadsBox" Width="80" Text="2"/>
            <Button x:Name="SaveSettingsButton" Content="Save Settings" Width="120" Margin="16,0,0,0" Click="SaveSettingsButton_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,0,0,12">
            <TextBlock Text="Allowed domains (one per line):" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBox x:Name="AllowedDomainsBox" Height="110" AcceptsReturn="True" TextWrapping="NoWrap" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Auto"/>
        </StackPanel>

        <Grid Grid.Row="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,6">
                <TextBlock Text="Clipboard URL:" FontWeight="SemiBold" Margin="0,0,8,0"/>
                <TextBox x:Name="ClipboardUrlBox" Width="540" IsReadOnly="True"/>
                <Button x:Name="ProcessClipboardButton" Content="Process" Width="90" Margin="8,0,0,0" Click="ProcessClipboardButton_Click"/>
            </StackPanel>

            <TextBox x:Name="LogBox" Grid.Row="1" IsReadOnly="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" TextWrapping="Wrap" AcceptsReturn="True"/>
        </Grid>
    </Grid>
</Window>
